<%= form_for @deck do |d| %>
  <% if @deck.errors.any? %>
    <div id='error_explanation'>
      <h2>
        <%= pluralize(@deck.errors.count, 'error') %> prohibited this article
        from being saved:
      </h2>
      <ul>
        <% @deck.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= d.text_field :name %><br>
  </p>
  
  <% for i in 1..15 do %>
    <p>
      <%= d.number_field("deck_#{i}", step: 1) %>
    </p>
  <% end %>
  <p>
    <%= d.submit %>
  </p>
<% end %>
